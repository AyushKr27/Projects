version: "3.8"
services:
  app:
    build: .
    ports:
      - "4000:4000"
    environment:
      - REDIS_ENABLED=false
    volumes:
      - ./:/app
    command: npm run dev
  redis:
    image: redis:7
    ports:
      - "6379:6379"
    restart: unless-stopped
